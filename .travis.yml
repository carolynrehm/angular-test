language: node_js
node_js:
  - "6.9"

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - ng build --prod
  - npm install sonar-scanner
  - sonar-scanner -Dsonar.projectKey=carolynrehm_angular-test -Dsonar.organization=carolynrehm-github -Dsonar.sources=src -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=58e430a8de4f420393ff425f71cfc822e7d825de
